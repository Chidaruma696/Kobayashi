<div>
  <%= f.label campo, etiqueta, class: "block text-sm font-medium" %>
  <%= f.send(tipo, campo, **{ class: "mt-1 w-full rounded border border-stone-300 px-3 py-2" }.merge(local_assigns[:opciones] || {})) %>
</div>
